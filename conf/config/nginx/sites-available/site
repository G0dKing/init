server {
  listen 80;
  listen [::]:80;
  server_name $DNS $WWW.DNS;

  location / {
    root /home/user/$DIR;
    index index.html;
  }

  location /app/ {
    proxy_pass http://localhost:3001;
    include /etc/nginx/proxy_headers;
  }
}
