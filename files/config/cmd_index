# CUSTOM COMMAND INDEX
# This file must be located in /$HOME/g0dking/files/config to use the new_cmd function.


# Command List
# ------------

# Reset SSH File Permissions
alias ssh_permit='cd ~ && chmod 600 ~/.ssh/* && chmod 700 ~/.ssh && chmod 644 ~/.ssh/*.pub'
