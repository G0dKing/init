#!/bin/bash

set_aliases() {
	alias c='clear'
	alias mkdir='mkdir -p'
	alias la='ls -a'
	alias ll='ls'
	alias cp='cp -r'
	alias rm='rm -rf'
	alias echo='echo -e'
    
    export set_aliases_complete=true
    
}
